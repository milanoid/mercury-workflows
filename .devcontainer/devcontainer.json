{
  "build": {
    "context": "..",
    "dockerfile": "Dockerfile"
  },
  "remoteEnv": {
      "SSH_AUTH_SOCK": "${localEnv:SSH_AUTH_SOCK}"
  },
  "postCreateCommand": "scripts/setup; echo 'alias k=kubectl' >> ~/.bash_aliases;",
  "forwardPorts": [
    3008
  ]
}
